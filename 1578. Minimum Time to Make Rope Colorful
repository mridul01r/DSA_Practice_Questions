1578. Minimum Time to Make Rope Colorful
link - https://leetcode.com/problems/minimum-time-to-make-rope-colorful/description/?envType=daily-question&envId=2025-11-03
Difficulty - Medium

Solution - O(Length of string)

class Solution {
    public int minCost(String colors, int[] neededTime) {
        int min = 0;
        int j = 0;
        int i = 1;
        int n = colors.length();
        while(i < n){
            
            if(colors.charAt(j) == colors.charAt(i)){
                min += Math.min(neededTime[i], neededTime[j]);
                neededTime[i] = Math.max(neededTime[i], neededTime[j]);
            }
            j = i;
            i++;  
        }
        return min;
    }
}
