1925. Count Square Sum Triples
Link - https://leetcode.com/problems/count-square-sum-triples/description/?envType=daily-question&envId=2025-12-08
Difficulty - Easy

Solution - O(n^3)
class Solution {
    public int countTriples(int n) {
        int cnt = 0;
        for (int c = 1; c <= n; c++) {
            int c2 = c * c;
            for (int b = 1; b < c; b++) {
                int b2 = b * b;
                for (int a = 1; a < b; a++) {
                    int a2 = a * a;
                    if (a2 + b2 == c2) {
                        cnt += 2;
                    }
                }
            }
        }
        return cnt;
    }
}

Solution - O(n*n*log(n))
class Solution {
    public int countTriples(int n) {
        int cnt = 0;
        for (int a = 1; a <= n; a++) {
            for (int b = a + 1; b <= n; b++) {
                int sumOfSquare = a * a + b * b;
                int c = (int) Math.sqrt(sumOfSquare);
                if (c * c == sumOfSquare && c <= n) {
                    cnt += 2;                }
            }
        }
        return cnt;
    }
}
